// TODO: Fase 3.1 — Ruta protegida: redirige a /login si no hay sesión activa
// Patrón de Guía 3 (SportClub): verifica token en AuthContext antes de renderizar
export default function ProtectedRoute() {
  return <div>Pendiente: ProtectedRoute</div>;
}
