/**
 * App.jsx — PNK Inmobiliaria
 * Punto de entrada de la SPA. Renderiza AppRoutes (toda la navegación).
 */

import AppRoutes from './routes/AppRoutes';

function App() {
  return <AppRoutes />;
}

export default App;
